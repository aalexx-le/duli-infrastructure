---
# ============================================================================
# CLOUDFLARE ACCESS CONFIGURATION
# ============================================================================
# Non-sensitive Cloudflare Access policy configuration.
# Secrets (API tokens) are stored in vault.yml
# Other settings (zone, tunnel name) are in setup_cloudflare.yml playbook vars
# ============================================================================

# Cloudflare Access - Allowed Users/Domains
# Format:
#   - "user@example.com" (specific email)
#   - "@yourdomain.com" (entire domain - allows all emails)
cloudflare_access_allowed_emails:
  - "engineer.duli@gmail.com"
  - "@gmail.com"
