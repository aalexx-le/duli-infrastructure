---
# Deploy Kubernetes cluster and core infrastructure
- import_playbook: kubespray.yml
- import_playbook: do_csi_driver.yml
- import_playbook: do_cloud_controller_manager.yml

# Deploy core infrastructure (Cert-Manager, operators, ArgoCD Applications)
- import_playbook: install_infrastructures.yml

# Configure DNS and SSL
- import_playbook: setup_dns.yml

# Generate sealed secrets (requires sealed-secrets controller)
- import_playbook: generate_sealed_secrets.yml

# Deploy applications with secret dependencies
- import_playbook: deploy_applications.yml

# Export connection information
- import_playbook: export_connection.yml

