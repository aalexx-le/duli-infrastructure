# Redis Instance - Base Configuration
# Environment-specific values are in values-staging.yaml and values-prod.yaml
redis:
  name: redis-replication
  mode: Replication
  redisSecret:
    name: redis-credentials
    key: password
